# images/ 下的 4K 原图（每张约 10 MB）随仓库一起部署，因为画廊「放大」
# 打开的就是原图本身；如果排除它们，线上点开会 404。
#
# 代价：每张原图会给仓库增加约 10 MB，点击放大时也要下载这么大的文件。
# 如果你更在意仓库体积和加载速度，可以让放大显示原生分辨率的衍生素材
# 而不是原图——那需要改 tools/build_gallery.py 重新加上 -full 输出，
# 并让 LIGHTBOX 指向它。视觉上几乎无差别，体积约为原图的 1/10。

# Python 运行 tools/ 下的脚本时产生的缓存
__pycache__/
*.py[cod]
